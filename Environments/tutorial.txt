EXPERIMENT 1 -- action-response delays. Three arms, same env, same seed, only --delay varies.

# each terminal
cd "/Users/gijsvdklink/Documents/Documents - MacBook Pro van Gijs/Gijs/Thesis/Thesis Gijs Github/Thesis-Gijs"
source ~/.venvs/RL/bin/activate
export OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1

# terminal 1  baseline (no delay)
python -m Training.v4_experiments_train --delay none          --seed 42 --n-envs 8

# terminal 2  deterministic (25 s first advisory, 12.5 s subsequent)
python -m Training.v4_experiments_train --delay deterministic --seed 42 --n-envs 8

# terminal 3  probabilistic (log-normal, same means)
python -m Training.v4_experiments_train --delay probabilistic --seed 42 --n-envs 8

# terminal 4  -> http://localhost:6006
tensorboard --logdir_spec none:Runs_saved/experiments/none,det:Runs_saved/experiments/deterministic,prob:Runs_saved/experiments/probabilistic


# cross-evaluation after training (train under one condition, test under another)
python -m Validation.cross_evaluate --runs Runs_saved/experiments --episodes 10
tensorboard --logdir Runs_saved/cross_eval

# watch a trained policy (--vecnorm is mandatory: raw-unit observations)
python -m visualisation.visualise \
    --model   "Runs_saved/experiments/none/v4exp_none_seed42_<ts>/checkpoints/best_model.zip" \
    --vecnorm "Runs_saved/experiments/none/v4exp_none_seed42_<ts>/checkpoints/best_model_vecnorm.pkl" \
    --n_ac 20 --density 8e-05
