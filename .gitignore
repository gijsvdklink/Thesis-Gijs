# Python cache
__pycache__/
*.pyc

# Large media files (exceed GitHub's 100 MB limit)
*.mp4

# Training output: TensorBoard event files and model checkpoints, written
# continuously during a run. Regenerate by training, not by cloning.
MAIN/Models/

# macOS
.DS_Store

# Claude tooling
.claude/

# Kept local only, not published to GitHub
LATEX/
VerticalCAS-master/
assignments/scratch.py
assignments/sandbox/

# Superseded models and report sources, parked until the new runs replace them
OLD/

# Evaluation output: per-episode CSVs and the figures built from them
MAIN/Validation/results/
MAIN/Validation/figures/
